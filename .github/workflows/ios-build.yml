name: ios-build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    env:
      IOS_P12_BASE64: ${{ secrets.IOS_P12_BASE64 }}
      IOS_P12_PASSWORD: ${{ secrets.IOS_P12_PASSWORD }}
      IOS_MOBILEPROVISION_BASE64: ${{ secrets.IOS_MOBILEPROVISION_BASE64 }}
      IOS_TEAM_ID: ${{ secrets.IOS_TEAM_ID }}
      IOS_BUNDLE_ID: ${{ secrets.IOS_BUNDLE_ID }}
      IOS_PROFILE_NAME: ${{ secrets.IOS_PROFILE_NAME }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build for simulator
        run: |
          xcodebuild \
            -project OpsAI.xcodeproj \
            -scheme OpsAI \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            build

      - name: Prepare signing assets
        if: ${{ env.IOS_P12_BASE64 != '' && env.IOS_MOBILEPROVISION_BASE64 != '' && env.IOS_TEAM_ID != '' && env.IOS_P12_PASSWORD != '' && env.IOS_BUNDLE_ID != '' && env.IOS_PROFILE_NAME != '' }}
        run: |
          mkdir -p $RUNNER_TEMP/signing
          echo "$IOS_P12_BASE64" | base64 --decode > $RUNNER_TEMP/signing/certificate.p12
          echo "$IOS_MOBILEPROVISION_BASE64" | base64 --decode > $RUNNER_TEMP/signing/profile.mobileprovision

          security create-keychain -p runner build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p runner build.keychain
          security import $RUNNER_TEMP/signing/certificate.p12 -k build.keychain -P "$IOS_P12_PASSWORD" -T /usr/bin/codesign
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp $RUNNER_TEMP/signing/profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      - name: Archive app
        if: ${{ env.IOS_P12_BASE64 != '' && env.IOS_MOBILEPROVISION_BASE64 != '' && env.IOS_TEAM_ID != '' && env.IOS_P12_PASSWORD != '' && env.IOS_BUNDLE_ID != '' && env.IOS_PROFILE_NAME != '' }}
        run: |
          xcodebuild \
            -project OpsAI.xcodeproj \
            -scheme OpsAI \
            -configuration Release \
            -archivePath $RUNNER_TEMP/OpsAI.xcarchive \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM=$IOS_TEAM_ID \
            PRODUCT_BUNDLE_IDENTIFIER=$IOS_BUNDLE_ID \
            PROVISIONING_PROFILE_SPECIFIER="$IOS_PROFILE_NAME" \
            archive

      - name: Export IPA
        if: ${{ env.IOS_P12_BASE64 != '' && env.IOS_MOBILEPROVISION_BASE64 != '' && env.IOS_TEAM_ID != '' && env.IOS_P12_PASSWORD != '' && env.IOS_BUNDLE_ID != '' && env.IOS_PROFILE_NAME != '' }}
        run: |
          cat > $RUNNER_TEMP/ExportOptions.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>ad-hoc</string>
            <key>signingStyle</key>
            <string>manual</string>
            <key>teamID</key>
            <string>${IOS_TEAM_ID}</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>${IOS_BUNDLE_ID}</key>
              <string>${IOS_PROFILE_NAME}</string>
            </dict>
            <key>stripSwiftSymbols</key>
            <true/>
          </dict>
          </plist>
          EOF

          xcodebuild \
            -exportArchive \
            -archivePath $RUNNER_TEMP/OpsAI.xcarchive \
            -exportOptionsPlist $RUNNER_TEMP/ExportOptions.plist \
            -exportPath $RUNNER_TEMP/export

      - name: Upload simulator build log context
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: opsai-build-context
          path: |
            OpsAI.xcodeproj
            project.yml

      - name: Upload IPA
        if: ${{ success() && env.IOS_P12_BASE64 != '' && env.IOS_MOBILEPROVISION_BASE64 != '' && env.IOS_TEAM_ID != '' && env.IOS_P12_PASSWORD != '' && env.IOS_BUNDLE_ID != '' && env.IOS_PROFILE_NAME != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: opsai-ipa
          path: ${{ runner.temp }}/export/*.ipa
